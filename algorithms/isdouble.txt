for a, b in V
A, B, C, D, #, $, @ not in V

alg IsDouble
    #a -> a#
    # -> B

    aCB -> DBa
    Ca -> aC

    ADa -> aAC
    aD -> Da

    ACB -> $@
    ADB -> $@

    @ab -> b@a
    $a -> $@a

    a$@a -> $
    $ ->.

    -> AC#
